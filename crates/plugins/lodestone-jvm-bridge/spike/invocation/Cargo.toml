[package]
name = "lodestone-jni-invocation-spike"
version = "0.0.0"
edition = "2024"
license = "GPL-3.0-or-later"
publish = false

[dependencies]
jni = { version = "=0.22.4", features = ["invocation"] }
lodestone-ecs = { path = "../../../../lodestone-ecs" }
lodestone-jvm-bridge = { path = "../.." }

# This is an executable experiment, not another production workspace member.
[workspace]
